---
alwaysApply: false
manual: true
---

# Error Handling Rules

**Note:** This rule provides specific guidance for error handling. For architectural error handling principles, see `nestjs-architecture.mdc` section 5.

## Exception Filters

Use Exception Filters for centralized error handling (see `nestjs-architecture.mdc` section 5):
- Create global exception filter
- Transform errors to consistent format
- Log errors appropriately

## Error Format

```json
{
  "error": {
    "message": "string",
    "code": "string",
    "details": "object (optional)"
  }
}
```

## Built-in Exceptions

Use NestJS built-in exceptions:
- `NotFoundException` - Resource not found
- `BadRequestException` - Invalid input data
- `UnauthorizedException` - Not authenticated
- `ForbiddenException` - Insufficient permissions
- `ConflictException` - Resource conflict
- `UnprocessableEntityException` - Validation error

## Custom Exceptions

Create custom exceptions when needed:
```typescript
export class RequestNotFoundException extends NotFoundException {
  constructor(id: string) {
    super(`Request with ID ${id} not found`);
  }
}
```

## Error Handling in Services

```typescript
async findOne(id: string): Promise<RequestDocument> {
  const request = await this.requestModel.findById(id).exec();
  
  if (!request) {
    throw new NotFoundException(`Request with ID ${id} not found`);
  }
  
  return request;
}
```

## Validation Errors

- Use ValidationPipe for automatic validation
- Return 422 Unprocessable Entity for validation errors
- Include field-level error details

## Logging

- Use NestJS Logger for error logging
- Log errors with context
- Don't log sensitive information (passwords, tokens)
