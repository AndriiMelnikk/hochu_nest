---
alwaysApply: false
manual: true
---

# Best Practices

**Note:** This rule provides additional best practices. For core architectural principles, see `nestjs-architecture.mdc` (always applied).

## TypeScript

- Use TypeScript strict mode
- Don't use `any` type, use `unknown` if needed
- Use proper types for all variables and functions
- Use interfaces and types appropriately

## Code Style

- Use async/await instead of Promises.then()
- Handle errors through try-catch or exceptions
- Use MongoDB sessions for complex operations requiring transactions
- Use dependency injection instead of creating instances manually
- Always call `.exec()` on Mongoose queries to get a Promise

## Constants & Configuration

- Don't hardcode values, use constants or configuration
- Use enum for statuses and roles
- Store configuration in environment variables
- Use @nestjs/config for configuration management

## Code Organization

- Follow DRY principle (Don't Repeat Yourself)
- Extract reusable logic into services or utilities
- Keep functions small and focused
- Use meaningful variable and function names

## Documentation

- Add JSDoc comments for complex methods
- Comment non-obvious business logic
- Document API endpoints with Swagger
- Keep README updated

## Git & Commits

- Use conventional commits:
  - `feat:` - New feature
  - `fix:` - Bug fix
  - `test:` - Adding tests
  - `refactor:` - Code refactoring
  - `docs:` - Documentation
  - `chore:` - Maintenance tasks

- Commit frequently, in small chunks
- Don't commit: node_modules, .env, dist/

## Performance

- Use MongoDB indexes for frequently queried fields
- Optimize database queries (avoid N+1 problems with populate)
- Use pagination for large datasets
- Use `.select()` to limit fields returned from queries
- Use `.lean()` for read-only queries to improve performance
- Cache frequently accessed data when appropriate
- Use aggregation pipeline for complex queries

## Security

- Never expose sensitive data in responses (use `.select()` to exclude fields)
- Hash passwords using bcrypt
- Validate all input data
- Use Mongoose schema validation and sanitization
- Validate ObjectId format before using in queries
- Implement rate limiting for API endpoints
- Use MongoDB's built-in protection against NoSQL injection (Mongoose handles this, but validate input)

## When Creating New Functionality

1. Create Mongoose schema in `database/schemas/` (if needed)
2. Register schema in module with `MongooseModule.forFeature()`
3. Create DTOs for validation
4. Create service with business logic (inject model with `@InjectModel`)
5. Create controller with endpoints
6. Add module to AppModule
7. Write tests (unit + e2e)
8. Add Swagger documentation
9. Check validation and error handling
10. Add indexes to schema for performance
